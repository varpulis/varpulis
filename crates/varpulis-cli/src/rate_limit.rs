//! Rate limiting module — re-exported from varpulis-cluster.
//!
//! The canonical implementation lives in `varpulis_cluster::rate_limit`.
//! This module re-exports all public items for backward compatibility.

pub use varpulis_cluster::rate_limit::*;
