apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: varpulis-coordinator
  namespace: varpulis
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: coordinator
          args:
            - "coordinator"
            - "--port"
            - "9100"
            - "--bind"
            - "0.0.0.0"
            - "--api-key"
            - "k3d-saas-test-key"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "500m"
