apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: varpulis-k3d
data:
  prometheus.yml: |
    global:
      scrape_interval: 5s

    scrape_configs:
      - job_name: 'varpulis'
        static_configs:
          - targets: ['varpulis:9090']
