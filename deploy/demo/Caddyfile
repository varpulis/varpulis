demo.varpulis-cep.com, www.varpulis-cep.com {
    tls /etc/caddy/certs/origin.pem /etc/caddy/certs/origin-key.pem

    # API requests -> cookie-sticky across 3 coordinators
    reverse_proxy /api/* coordinator-1:9100 coordinator-2:9100 coordinator-3:9100 {
        lb_policy cookie
        health_uri /health
        health_interval 10s
    }

    # WebSocket -> cookie-sticky across 3 coordinators
    reverse_proxy /ws coordinator-1:9100 coordinator-2:9100 coordinator-3:9100 {
        lb_policy cookie
        health_uri /health
        health_interval 10s
    }

    # Grafana dashboard (matches /grafana and /grafana/*)
    @grafana path /grafana /grafana/*
    reverse_proxy @grafana grafana:3000

    # Auth endpoints -> coordinators (for login/signup)
    reverse_proxy /auth/* coordinator-1:9100 coordinator-2:9100 coordinator-3:9100 {
        lb_policy cookie
        health_uri /health
        health_interval 10s
    }

    # Documentation -> docs container (VitePress static site under /docs/)
    @docs path /docs /docs/*
    reverse_proxy @docs docs:80

    # SEO files -> docs container (sitemap, robots.txt at root)
    @seo path /sitemap.xml /robots.txt
    reverse_proxy @seo docs:80

    # Stream Builder (visual pipeline editor)
    @streambuilder path /stream-builder /stream-builder/*
    handle @streambuilder {
        uri strip_prefix /stream-builder
        reverse_proxy stream-builder:8080
    }

    # Everything else -> web-ui (SPA with all routes: landing, auth, control plane)
    reverse_proxy /* web-ui:8080
}

# Blog subdomain -> redirect root to /blog, proxy everything to web-ui
blog.varpulis-cep.com {
    tls /etc/caddy/certs/origin.pem /etc/caddy/certs/origin-key.pem
    @root path /
    redir @root https://blog.varpulis-cep.com/blog temporary
    reverse_proxy web-ui:8080
}

# Docs subdomain -> redirect to main domain /docs/
docs.varpulis-cep.com {
    tls /etc/caddy/certs/origin.pem /etc/caddy/certs/origin-key.pem
    redir https://www.varpulis-cep.com/docs{uri} permanent
}

# Register subdomain -> redirect root to /signup, proxy everything to web-ui
register.varpulis-cep.com {
    tls /etc/caddy/certs/origin.pem /etc/caddy/certs/origin-key.pem
    @root path /
    redir @root https://register.varpulis-cep.com/signup temporary
    reverse_proxy web-ui:8080
}
